#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"

if [[ $# -eq 0 ]]; then
  mcc_fail "Usage: ./bin/phpcbf <path...>"
fi

mcc_run_host_phpcs_tool "${PHPCBF_BIN}" "phpcbf" "PHPCBF_BIN" "$@"
